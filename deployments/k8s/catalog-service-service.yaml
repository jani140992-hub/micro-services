apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: cloudmart
  labels:
    app.kubernetes.io/name: catalog-service
    app.kubernetes.io/part-of: cloudmart
spec:
  type: ClusterIP
  ports:
    - port: 8003
      targetPort: 8003
      name: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: catalog-service
