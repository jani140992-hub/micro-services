apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: cloudmart
  labels:
    app.kubernetes.io/name: payment-service
    app.kubernetes.io/part-of: cloudmart
spec:
  type: ClusterIP
  ports:
    - port: 8006
      targetPort: 8006
      name: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: payment-service
