apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: cloudmart
  labels:
    app.kubernetes.io/name: inventory-service
    app.kubernetes.io/part-of: cloudmart
spec:
  type: ClusterIP
  ports:
    - port: 8004
      targetPort: 8004
      name: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: inventory-service
