apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: cloudmart
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: cloudmart
spec:
  type: ClusterIP
  ports:
    - port: 8005
      targetPort: 8005
      name: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: order-service
