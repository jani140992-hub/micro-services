apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: cloudmart
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: cloudmart
spec:
  type: ClusterIP
  ports:
    - port: 8002
      targetPort: 8002
      name: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: user-service
